--! SECURE_MARKER (Keep this line EXACTLY as-is)
local realURL = "https://raw.githubusercontent.com/YOURNAME/REPO/main/loader.lua"
local check = game:HttpGet(realURL) -- Script MUST contact GitHub

-- If skids copy-paste, this FAILS:
if not check or not check:find("SECURE_MARKER") then
    error("❌ Use the GitHub link! (Copied scripts won't work)") 
end

-- Load real script (keep main.lua obfuscated)
loadstring(game:HttpGet("https://raw.githubusercontent.com/YOURNAME/REPO/main/main.lua"))()
